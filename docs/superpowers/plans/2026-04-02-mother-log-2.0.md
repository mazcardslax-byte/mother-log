# Mother Log 2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply the Carbon Amber design system across all tabs for a polished "large-scale operation" feel ahead of a boss demo.

**Architecture:** Add CSS custom properties + Tailwind color aliases, then do a targeted visual pass on each file — shared components first, then tab contents. No data model changes, no new files, no logic changes.

**Tech Stack:** React 18, Vite, Tailwind CSS v3, Lucide React, Supabase (unchanged)

---

## Color Substitution Map

Use this as a reference throughout every task. When you see a class on the LEFT, replace it with the class on the RIGHT. **Do not bulk search-replace across the whole file — apply changes component by component so you can verify visually.**

### Backgrounds / Surfaces
| Before | After |
|--------|-------|
| `bg-[#080c09]` | `bg-[#0a0a0a]` |
| `bg-[#0e1512]` (sheet bg) | `bg-[#0f0f0f]` |
| `bg-zinc-900` | `bg-[#111111]` |
| `bg-zinc-900/70`, `bg-zinc-900/80` | `bg-[#111111]` |
| `bg-zinc-800` | `bg-[#1a1a1a]` |
| `bg-zinc-800/60` | `bg-[#1a1a1a]/60` |

### Borders
| Before | After |
|--------|-------|
| `border-zinc-800`, `border-zinc-800/80`, `border-zinc-800/50` | `border-[#2a2418]` |
| `border-zinc-700`, `border-zinc-700/50` | `border-[#2a2418]` |

### Text
| Before | After |
|--------|-------|
| `text-white` | `text-[#f5f5f0]` |
| `text-zinc-200` | `text-[#f5f5f0]` |
| `text-zinc-300` | `text-[#c5b08a]` |
| `text-zinc-400` | `text-[#c5b08a]` |
| `text-zinc-500` | `text-[#6a5a3a]` |
| `text-zinc-600` | `text-[#6a5a3a]` |

### Accent (emerald UI → amber) — only UI accent, NOT semantic health colors
| Before | After |
|--------|-------|
| `bg-emerald-700` (button) | `bg-amber-600` |
| `hover:bg-emerald-600` | `hover:bg-amber-500` |
| `active:bg-emerald-800` | `active:bg-amber-700` |
| `bg-emerald-800/50` (active tab) | `bg-[#2a1f00]` |
| `border-emerald-700/30` (active tab border) | `border-[#3a2e00]` |
| `text-emerald-200` (active tab text) | `text-amber-300` |
| `text-emerald-300` (active tab icon) | `text-amber-400` |
| `text-emerald-700` (branding subtitle) | `text-amber-700` |
| `text-emerald-400` (logo icon) | `text-amber-400` |
| `text-emerald-600` (sync icon) | `text-amber-600` |
| `shadow-emerald-950/60` | `shadow-amber-950/60` |
| `focus:border-emerald-600` | `focus:border-amber-500` |
| `focus-visible:ring-emerald-500/40` | `focus-visible:ring-amber-500/40` |

### Keep unchanged (semantic)
- Mother health levels: `text-red-400`, `text-yellow-400`, `text-emerald-400` — these express health state, not brand
- Mother status badge: `bg-emerald-900/50 text-emerald-300 border-emerald-700/40` (Active) — semantic
- Health left borders: `border-l-emerald-500` — semantic
- TYPE_META event colors: sky (transplant), violet (amendment), emerald (feeding), red (reduction) — keep per spec
- StatsTab `rateColor()` and `healthColor()` hex values — semantic data colors
- Clone status badge colors (Cloned/Transplanted/etc.) — keep

---

## Task 1: Design Tokens

**Files:**
- Modify: `src/index.css`
- Modify: `tailwind.config.js`

- [ ] **Step 1: Add CSS custom properties to `src/index.css`**

Insert after the `@tailwind utilities;` line:

```css
/* ── Carbon Amber Design Tokens ───────────────────────────────────────────── */
:root {
  --color-bg:           #0a0a0a;
  --color-surface:      #111111;
  --color-border:       #2a2418;
  --color-amber:        #fbbf24;
  --color-amber-2:      #fcd34d;
  --color-amber-3:      #fde68a;
  --color-amber-bg:     #2a1f00;
  --color-amber-border: #3a2e00;
  --color-text-primary: #f5f5f0;
  --color-text-body:    #c5b08a;
  --color-text-muted:   #6a5a3a;
}
```

- [ ] **Step 2: Extend Tailwind config with Carbon Amber aliases**

Replace the contents of `tailwind.config.js` with:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        ca: {
          bg:           "#0a0a0a",
          surface:      "#111111",
          border:       "#2a2418",
          "amber-bg":   "#2a1f00",
          "amber-border": "#3a2e00",
          text:         "#f5f5f0",
          body:         "#c5b08a",
          muted:        "#6a5a3a",
        },
      },
    },
  },
  plugins: [],
}
```

- [ ] **Step 3: Verify build still passes**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: build completes with no errors.

- [ ] **Step 4: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/index.css tailwind.config.js
git commit -m "feat: add Carbon Amber design tokens"
```

---

## Task 2: Shared Components in App.jsx

These are module-level constants and small components used everywhere. Fix them first so all subsequent tasks inherit the right styles.

**Files:**
- Modify: `src/App.jsx` (lines ~253–295, ~809–830, ~747–756)

- [ ] **Step 1: Update `BottomSheet` component (~line 253–261)**

Find the `BottomSheet` function. Change:
```jsx
<div className="bg-[#0e1512] border border-zinc-700/50 rounded-t-3xl ...">
  ...
  <div className="flex items-center justify-between px-5 py-3 border-b border-zinc-800/80 ...">
    <span className="text-white font-semibold text-sm">{title}</span>
    <button ... className="... text-zinc-500 active:text-white active:bg-zinc-800 ...">✕</button>
```
To:
```jsx
<div className="bg-[#0f0f0f] border border-[#2a2418] rounded-t-3xl ...">
  ...
  <div className="flex items-center justify-between px-5 py-3 border-b border-[#2a2418] ...">
    <span className="text-[#f5f5f0] font-semibold text-sm">{title}</span>
    <button ... className="... text-[#6a5a3a] active:text-[#f5f5f0] active:bg-[#1a1a1a] ...">✕</button>
```

- [ ] **Step 2: Update `StatChip` component (~line 271–275)**

Find the `StatChip` function. Change:
```jsx
<div className="bg-zinc-900/80 border border-zinc-800/80 rounded-2xl p-3.5 text-center">
  <div className={`text-2xl font-bold tracking-tight ${colorClass}`}>{value}</div>
  <div className="text-[10px] text-zinc-500 mt-1 leading-tight font-medium uppercase tracking-wide">{label}</div>
  {sub && <div className="text-[10px] text-zinc-500 mt-0.5">{sub}</div>}
```
To:
```jsx
<div className="bg-[#111111] border border-[#2a2418] rounded-xl p-3.5 text-center">
  <div className={`text-2xl font-bold tracking-tight ${colorClass}`}>{value}</div>
  <div className="text-[10px] text-[#6a5a3a] mt-1 leading-tight font-medium uppercase tracking-wide">{label}</div>
  {sub && <div className="text-[10px] text-[#6a5a3a] mt-0.5">{sub}</div>}
```

- [ ] **Step 3: Update `SectionLabel` component (~line 279–281)**

```jsx
// Before
function SectionLabel({ children }) {
  return <div className="text-[10px] text-zinc-600 uppercase tracking-widest font-bold mb-2 px-0.5">{children}</div>;
}

// After
function SectionLabel({ children }) {
  return <div className="text-[10px] text-[#6a5a3a] uppercase tracking-widest font-bold mb-2 px-0.5">{children}</div>;
}
```

- [ ] **Step 4: Update `FieldRow` component (~line 284–289)**

Find `FieldRow`. Change:
```jsx
// Before
<label className="block text-xs text-zinc-400 mb-1.5 font-medium">{label}</label>

// After
<label className="block text-xs text-[#c5b08a] mb-1.5 font-medium">{label}</label>
```

- [ ] **Step 5: Update shared class constants (~lines 292–295)**

```jsx
// Before
const inputCls = "w-full bg-zinc-800 border border-zinc-700 rounded-xl px-3 py-2.5 text-sm text-zinc-200 placeholder-zinc-600 focus:outline-none focus:border-emerald-600 focus-visible:ring-2 focus-visible:ring-emerald-500/40";
const selectCls = "w-full bg-zinc-800 border border-zinc-700 rounded-xl px-3 py-2.5 text-sm text-zinc-200 focus:outline-none focus:border-emerald-600 focus-visible:ring-2 focus-visible:ring-emerald-500/40";
const btnPrimary = "w-full bg-emerald-700 hover:bg-emerald-600 active:bg-emerald-800 text-white font-semibold text-sm rounded-xl py-3 transition-colors min-h-[44px]";
const btnSecondary = "w-full bg-zinc-800 hover:bg-zinc-700 active:bg-zinc-900 text-zinc-300 font-medium text-sm rounded-xl py-2.5 transition-colors border border-zinc-700 min-h-[44px]";

// After
const inputCls = "w-full bg-[#1a1a1a] border border-[#2a2418] rounded-xl px-3 py-2.5 text-sm text-[#f5f5f0] placeholder-[#6a5a3a] focus:outline-none focus:border-amber-500 focus-visible:ring-2 focus-visible:ring-amber-500/40";
const selectCls = "w-full bg-[#1a1a1a] border border-[#2a2418] rounded-xl px-3 py-2.5 text-sm text-[#f5f5f0] focus:outline-none focus:border-amber-500 focus-visible:ring-2 focus-visible:ring-amber-500/40";
const btnPrimary = "w-full bg-amber-600 hover:bg-amber-500 active:bg-amber-700 text-[#0a0a0a] font-semibold text-sm rounded-xl py-3 transition-colors min-h-[44px]";
const btnSecondary = "w-full bg-[#1a1a1a] hover:bg-[#222] active:bg-[#111] text-[#c5b08a] font-medium text-sm rounded-xl py-2.5 transition-colors border border-[#2a2418] min-h-[44px]";
```

- [ ] **Step 6: Update loading screen (~lines 747–756)**

Find the `if (loading)` block. Change:
```jsx
// Before
<div className="min-h-screen bg-[#080c09] flex flex-col items-center justify-center gap-4">
  <div className="w-16 h-16 rounded-2xl bg-emerald-900/40 border border-emerald-800/60 ...">
    <Leaf className="w-8 h-8 text-emerald-400" strokeWidth={1.5} />
  </div>
  <div className="text-white font-bold text-xl ...">Mother Log</div>
  <div className="text-emerald-700 text-[11px] ...">Stacks Family Farms</div>
  <Loader2 className="w-4 h-4 text-emerald-700 animate-spin mt-2" />

// After
<div className="min-h-screen bg-[#0a0a0a] flex flex-col items-center justify-center gap-4">
  <div className="w-16 h-16 rounded-2xl bg-[#2a1f00] border border-[#3a2e00] ...">
    <Leaf className="w-8 h-8 text-amber-400" strokeWidth={1.5} />
  </div>
  <div className="text-[#f5f5f0] font-bold text-xl ...">Mother Log</div>
  <div className="text-amber-700 text-[11px] ...">Stacks Family Farms</div>
  <Loader2 className="w-4 h-4 text-amber-700 animate-spin mt-2" />
```

- [ ] **Step 7: Update app root and header (~lines 764–806)**

```jsx
// Root div: bg-[#080c09] → bg-[#0a0a0a], text-zinc-300 → text-[#c5b08a]
<div className="min-h-screen bg-[#0a0a0a] text-[#c5b08a] max-w-md mx-auto flex flex-col pb-4">

// Header border: border-zinc-800/50 → border-[#2a2418]
<div className="px-4 pt-safe pb-3 border-b border-[#2a2418]">

// Logo icon container: bg-emerald-900/50 border-emerald-800/50 → bg-[#2a1f00] border-[#3a2e00]
<div className="w-9 h-9 rounded-xl bg-[#2a1f00] border border-[#3a2e00] ...">
  <Leaf className="w-[18px] h-[18px] text-amber-400" strokeWidth={1.75} />

// App title: text-white → text-[#f5f5f0]
<h1 className="text-[#f5f5f0] font-bold text-base ...">Mother Log</h1>

// Subtitle: text-emerald-700 → text-amber-700
<p className="text-amber-700 text-[10px] ...">Stacks Family Farms</p>

// Sync icon colors:
syncStatus === "syncing" ? "text-yellow-400 animate-spin" :
syncStatus === "error"   ? "text-red-500" :
                          "text-amber-600"   // was text-emerald-600

// Settings button: border-zinc-800 text-zinc-500 → border-[#2a2418] text-[#6a5a3a]
className="... border border-[#2a2418] text-[#6a5a3a] active:text-[#f5f5f0] active:bg-[#1a1a1a] ..."

// Add button: bg-emerald-700 active:bg-emerald-800 shadow-emerald-950/60 → bg-amber-600 active:bg-amber-700 shadow-amber-950/60
className="... bg-amber-600 active:bg-amber-700 text-[#0a0a0a] ... shadow-amber-950/60"
```

- [ ] **Step 8: Update tab bar (~lines 810–829)**

```jsx
// Tab bar container: bg-zinc-900/70 border-zinc-800/80 → bg-[#111111] border-[#2a2418]
<div className="flex gap-0.5 bg-[#111111] border border-[#2a2418] rounded-2xl p-1">

// Active tab: bg-emerald-800/50 text-emerald-200 border-emerald-700/30 → bg-[#2a1f00] text-amber-300 border-[#3a2e00]
// Inactive tab: text-zinc-600 active:text-zinc-300 active:bg-zinc-800/50 → text-[#6a5a3a] active:text-[#c5b08a] active:bg-[#1a1a1a]
active
  ? "bg-[#2a1f00] text-amber-300 border border-[#3a2e00] shadow-sm"
  : "text-[#6a5a3a] active:text-[#c5b08a] active:bg-[#1a1a1a]"

// Active icon: text-emerald-300 → text-amber-400
// Inactive icon: text-zinc-500 → text-[#6a5a3a]
<Icon className={`w-[18px] h-[18px] ${active ? "text-amber-400" : "text-[#6a5a3a]"}`} strokeWidth={active ? 2 : 1.75} />

// Tab label:
<span className={`text-[10px] font-semibold tracking-wide ${active ? "text-amber-300" : "text-[#6a5a3a]"}`}>{label}</span>
```

- [ ] **Step 9: Verify build**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 10: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/App.jsx
git commit -m "feat: Carbon Amber pass — shared components + tab bar"
```

---

## Task 3: Summary + Mothers Tabs in App.jsx

**Files:**
- Modify: `src/App.jsx` (~lines 940–1200 for SummaryTab, ~1200–1800 for MothersTab/MotherDetailModal)

This is the largest visual task. Apply the color substitution map to:
- `SummaryTab` component
- `MothersTab` component  
- `MotherCard` component (or however individual mother rows are rendered)
- `MotherDetailModal` component

- [ ] **Step 1: Update `SummaryTab` (~lines 940–1100)**

Key changes to make throughout SummaryTab:
- All `bg-zinc-900*` card backgrounds → `bg-[#111111]`
- All `border-zinc-800*` → `border-[#2a2418]`
- All `text-zinc-500`, `text-zinc-600` muted text → `text-[#6a5a3a]`
- All `text-zinc-300`, `text-zinc-200` body text → `text-[#c5b08a]` / `text-[#f5f5f0]`
- Health-level badge bg (`bg-emerald-900/40 border-emerald-700/40 text-emerald-300` at health=5) — **KEEP**, it's semantic

Example — the health breakdown table:
```jsx
// Before
<div className="bg-zinc-900/80 border border-zinc-800/80 rounded-2xl overflow-hidden">
  <div className="flex items-center justify-between px-4 py-2.5 border-b border-zinc-800/50 last:border-0">

// After
<div className="bg-[#111111] border border-[#2a2418] rounded-2xl overflow-hidden">
  <div className="flex items-center justify-between px-4 py-2.5 border-b border-[#2a2418] last:border-0">
```

Strain analytics cards:
```jsx
// Before
<div className="bg-zinc-900/80 border border-zinc-800/80 rounded-2xl px-4 py-3 ...">

// After
<div className="bg-[#111111] border border-[#2a2418] rounded-2xl px-4 py-3 ...">
```

- [ ] **Step 2: Update `MothersTab` — mother list cards**

The mother card buttons (press-card style):
```jsx
// Before
<button className="press-card w-full bg-zinc-900 border border-red-900/50 rounded-2xl px-4 py-3.5 text-left">
<button className="press-card w-full bg-zinc-900/80 border border-zinc-800 rounded-2xl px-4 py-3.5 text-left">

// After
<button className="press-card w-full bg-[#111111] border border-red-900/50 rounded-2xl px-4 py-3.5 text-left">
<button className="press-card w-full bg-[#111111] border border-[#2a2418] rounded-2xl px-4 py-3.5 text-left">
```

Empty state:
```jsx
// Before
<div className="bg-zinc-900 border border-zinc-800 rounded-2xl p-8 ...">
  <div className="text-[10px] text-zinc-600 uppercase tracking-wider mb-2">What you can track</div>

// After
<div className="bg-[#111111] border border-[#2a2418] rounded-2xl p-8 ...">
  <div className="text-[10px] text-[#6a5a3a] uppercase tracking-wider mb-2">What you can track</div>
```

- [ ] **Step 3: Update `MotherDetailModal` — history entries**

Event history uses TYPE_META for left-border accents — those are kept. The card containers:
```jsx
// All history entry card wrappers: bg-zinc-900/80 border border-zinc-800 → bg-[#111111] border border-[#2a2418]
// Any text-zinc-* → apply substitution map
```

- [ ] **Step 4: Build check**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/App.jsx
git commit -m "feat: Carbon Amber pass — Summary + Mothers tabs"
```

---

## Task 4: Room + Facility + Add Mother Tabs in App.jsx

**Files:**
- Modify: `src/App.jsx` (RoomTab, FacilityTab, AddMotherTab components)

- [ ] **Step 1: Update `RoomTab` component**

Apply the substitution map:
- All `bg-zinc-900*` → `bg-[#111111]`
- All `border-zinc-800*` → `border-[#2a2418]`
- All `text-zinc-*` → apply map
- `text-sky-300` (container labels in room grid) — **KEEP**, semantic
- `bg-sky-600` (progress bar) — **KEEP**, semantic

- [ ] **Step 2: Update `FacilityTab` component**

Apply the substitution map. Facility items are maintenance logs — no semantic health colors here, so apply all substitutions fully.

- [ ] **Step 3: Update `AddMotherTab` component**

Form inputs already use `inputCls`/`selectCls`/`btnPrimary`/`btnSecondary` constants (updated in Task 2), so most of this is automatic. Check for any inline styles or classes not using those constants and apply the substitution map.

- [ ] **Step 4: Build check**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/App.jsx
git commit -m "feat: Carbon Amber pass — Room, Facility, Add tabs"
```

---

## Task 5: StatsTab.jsx

**Files:**
- Modify: `src/StatsTab.jsx`

- [ ] **Step 1: Update `SectionLabel` component (~line 36–40)**

```jsx
// Before
function SectionLabel({ children }) {
  return (
    <div className="text-[10px] font-semibold tracking-widest text-zinc-500 uppercase mb-3">
      {children}
    </div>
  );
}

// After
function SectionLabel({ children }) {
  return (
    <div className="text-[10px] font-semibold tracking-widest text-[#6a5a3a] uppercase mb-3">
      {children}
    </div>
  );
}
```

- [ ] **Step 2: Update stat chip (~line 63–65)**

```jsx
// Before
<div key={label} className="flex-1 bg-zinc-900 border border-zinc-800 rounded-xl p-3 text-center">
  <div className="text-xl font-bold" style={style}>{value}</div>
  <div className="text-[10px] text-zinc-500 mt-0.5">{label}</div>

// After
<div key={label} className="flex-1 bg-[#111111] border border-[#2a2418] rounded-xl p-3 text-center">
  <div className="text-xl font-bold" style={style}>{value}</div>
  <div className="text-[10px] text-[#6a5a3a] mt-0.5">{label}</div>
```

- [ ] **Step 3: Update strain comparison table (~lines 74, 138, 147–230)**

Apply substitution map throughout:
```jsx
// Card containers:
// bg-zinc-900 border border-zinc-800 → bg-[#111111] border border-[#2a2418]

// Table header row:
// text-zinc-600 → text-[#6a5a3a]
// Active sort column text-emerald-400 → text-amber-400

// Table body rows:
// border-zinc-800/60 → border-[#2a2418]
// text-zinc-300 → text-[#c5b08a]

// Strain filter select:
// bg-zinc-900 border border-zinc-700 ... text-zinc-200 → bg-[#111111] border border-[#2a2418] ... text-[#f5f5f0]
```

- [ ] **Step 4: Update care gaps list (~lines 259–265)**

```jsx
// Before
<div className={`bg-zinc-900 border rounded-xl px-4 py-3 ... ${
  g.daysSinceNum > 10 ? "border-red-900/60" : g.daysSinceNum > 6 ? "border-yellow-900/60" : "border-zinc-800"
}`}>
  <div className="text-sm font-semibold text-zinc-200">...</div>
  <div className="text-xs text-zinc-500 mt-0.5">...

// After
<div className={`bg-[#111111] border rounded-xl px-4 py-3 ... ${
  g.daysSinceNum > 10 ? "border-red-900/60" : g.daysSinceNum > 6 ? "border-yellow-900/60" : "border-[#2a2418]"
}`}>
  <div className="text-sm font-semibold text-[#f5f5f0]">...</div>
  <div className="text-xs text-[#6a5a3a] mt-0.5">...
```

- [ ] **Step 5: Note on `rateColor()` and `healthColor()` hex values**

These functions return inline hex colors for chart bars/cells (`#10b981` green, `#f59e0b` yellow, `#ef4444` red). **Leave them unchanged** — they are semantic data encoding on chart elements, not brand UI.

- [ ] **Step 6: Build check**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/StatsTab.jsx
git commit -m "feat: Carbon Amber pass — StatsTab"
```

---

## Task 6: DryRoomTab.jsx

**Files:**
- Modify: `src/DryRoomTab.jsx`

DryRoomTab has 3 sub-tabs (Hanging, Archive, Bins). Apply the full substitution map to all three.

- [ ] **Step 1: Update sub-tab toggle (~line 831–841)**

```jsx
// Before
view === v
  ? "bg-emerald-900/60 text-emerald-300 border border-emerald-700/50"
  : "text-zinc-500 hover:text-zinc-300 border border-transparent"

// After
view === v
  ? "bg-[#2a1f00] text-amber-300 border border-[#3a2e00]"
  : "text-[#6a5a3a] hover:text-[#c5b08a] border border-transparent"
```

- [ ] **Step 2: Update the main DryRoom sub-tab bar**

Find `DRY_ROOM_SUB_TABS` toggle (Hanging / Archive / Bins). Apply same active/inactive pattern as above.

- [ ] **Step 3: Apply substitution map to `HangingPanel` component**

Card containers, text, borders — apply map. Keep any semantic health/urgency colors (`text-red-*`, `text-yellow-*`, countdown colors from `countdownColor()` utility — those are semantic).

- [ ] **Step 4: Apply substitution map to `BinsPanel` component (~line 828+)**

The active/archive sub-toggle in BinsPanel was the first place you found the emerald sub-tab style (already replaced in Step 1). Apply map to all bin cards and the `BinCard` component.

- [ ] **Step 5: Apply substitution map to `ArchivePanel` and `HarvestCard`**

- [ ] **Step 6: Build check**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
cd C:/Users/zacho/mother-log
git add src/DryRoomTab.jsx
git commit -m "feat: Carbon Amber pass — DryRoomTab (incl. Bins)"
```

---

## Task 7: ClonesTab.jsx

**Files:**
- Modify: `src/ClonesTab.jsx`

- [ ] **Step 1: Update `BottomSheet` copy in ClonesTab (~line 138–147)**

ClonesTab has its own `BottomSheet` component. Apply same changes as Task 2 Step 1:
```jsx
// bg-[#0e1512] → bg-[#0f0f0f]
// border-zinc-700/50 → border-[#2a2418]
// bg-zinc-700 (handle) → bg-[#2a2418]
// text-white → text-[#f5f5f0]
// text-zinc-500 → text-[#6a5a3a]
// hover:bg-zinc-700/50 → hover:bg-[#1a1a1a]
```

- [ ] **Step 2: Update `StatCard` component (~line 158–163)**

```jsx
// Before
<div className="bg-zinc-900 border border-zinc-800 rounded-xl p-3 text-center">
  <div className={`text-2xl font-bold ${c[color] || "text-zinc-200"}`}>{value}</div>
  <div className="text-[10px] text-zinc-500 mt-0.5">{label}</div>
  {sub && <div className="text-[10px] text-zinc-700 mt-0.5">{sub}</div>}

// After
<div className="bg-[#111111] border border-[#2a2418] rounded-xl p-3 text-center">
  <div className={`text-2xl font-bold ${c[color] || "text-[#f5f5f0]"}`}>{value}</div>
  <div className="text-[10px] text-[#6a5a3a] mt-0.5">{label}</div>
  {sub && <div className="text-[10px] text-[#6a5a3a] mt-0.5">{sub}</div>}
```

Also update the `c` color map in StatCard:
```jsx
// Before
const c = { green: "text-emerald-400", sky: "text-sky-400" };
// After — green maps to amber for clone counts, sky stays for transplanted
const c = { green: "text-amber-400", sky: "text-sky-400" };
```

- [ ] **Step 3: Apply substitution map to `PlantGrid` and plant detail sections (~line 170+)**

Key changes:
```jsx
// Section label: text-zinc-600 → text-[#6a5a3a]
// Empty state: bg-zinc-900 border border-zinc-800 → bg-[#111111] border border-[#2a2418], text-zinc-600 → text-[#6a5a3a]
// Plant grid container: bg-zinc-900 border border-zinc-800 → bg-[#111111] border border-[#2a2418]
// Grid gap background: bg-zinc-800 → bg-[#2a2418]
// Individual plant cells: bg-zinc-900 → bg-[#111111]
// Plant name: text-zinc-300 → text-[#c5b08a]
// Count: text-zinc-500 → text-[#6a5a3a]
// Strain filter select: bg-zinc-800 border border-zinc-700 text-white → bg-[#1a1a1a] border border-[#2a2418] text-[#f5f5f0]
// Batch info strip: bg-zinc-800/60 text-zinc-500 text-zinc-400 → bg-[#1a1a1a]/60 text-[#6a5a3a] text-[#c5b08a]
// Round selector inactive: bg-zinc-800 border-zinc-700 text-zinc-500 → bg-[#1a1a1a] border-[#2a2418] text-[#6a5a3a]
```

Clone status badge colors (`Cloned`, `Transplanted`, `Upcoming`, `Next`, `Archived`) are semantic state — **leave unchanged**.

- [ ] **Step 4: Apply map to all remaining form/modal components in ClonesTab**

Any remaining `bg-zinc-*`, `border-zinc-*`, `text-zinc-*`, `bg-emerald-*` that are UI chrome (not semantic data) — apply map.

- [ ] **Step 5: Build check**

```bash
cd C:/Users/zacho/mother-log && npm run build
```

Expected: no errors.

- [ ] **Step 6: Final visual verification**

Start dev server:
```bash
cd C:/Users/zacho/mother-log && npm run dev
```

Open http://localhost:5173 and check:
- [ ] Loading screen shows amber icon
- [ ] Tab bar has amber active pill
- [ ] All 7 tabs use `#111111` card surfaces and `#2a2418` borders
- [ ] Header branding shows amber accent
- [ ] Forms: inputs/selects use dark bg with amber focus ring
- [ ] Primary buttons are amber, not green
- [ ] Health/status colors (green Active badge, red/yellow health indicators) still intact
- [ ] Dry Room Bins sub-tab works correctly

- [ ] **Step 7: Final commit**

```bash
cd C:/Users/zacho/mother-log
git add src/ClonesTab.jsx
git commit -m "feat: Carbon Amber pass — ClonesTab"
```

---

## Task 8: Deploy

- [ ] **Step 1: Push to main**

```bash
cd C:/Users/zacho/mother-log && git push origin main
```

Vercel auto-deploys on push. Check the deployment completes at the Vercel dashboard.

- [ ] **Step 2: Smoke test on mobile**

Open the deployed PWA URL on a phone and verify:
- App loads without flash of old green colors
- All tabs scroll and interact correctly
- Add/edit modals open and close
- Dry Room Bins sub-tab accessible
